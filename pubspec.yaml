name: bitcoin_ticker
description: A new Flutter application.
version: 1.0.0+1
environment:
  sdk: ">=2.12.0 <=3.0.5"

dependencies:
  cupertino_icons: ^1.0.5
  flutter:
    sdk: flutter
  http: ^1.1.0


dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true

